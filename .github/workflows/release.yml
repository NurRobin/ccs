name: Release & APT Repo

on:
  push:
    branches: [main]
    paths: [ccs]
  workflow_dispatch:

permissions:
  contents: write
  pages: write

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      created: ${{ steps.check.outputs.created }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep -oP 'CCS_VERSION="\K[^"]+' ccs)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Check if release exists
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "v${{ steps.version.outputs.version }}" &>/dev/null; then
            echo "Release v${{ steps.version.outputs.version }} already exists"
            echo "created=false" >> "$GITHUB_OUTPUT"
          else
            echo "created=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Build .deb
        if: steps.check.outputs.created == 'true'
        run: bash build-deb.sh

      - name: Create release
        if: steps.check.outputs.created == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          git tag "v${VERSION}"
          git push origin "v${VERSION}"
          gh release create "v${VERSION}" \
            --title "v${VERSION}" \
            --generate-notes \
            dist/ccs_${VERSION}_all.deb

  update-apt-repo:
    needs: release
    if: needs.release.outputs.created == 'true' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: repo
        continue-on-error: true

      - name: Init gh-pages if needed
        run: |
          if [ ! -d repo/.git ]; then
            mkdir -p repo
            cd repo
            git init
            git checkout -b gh-pages
          fi

      - name: Import GPG key
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | gpg --batch --import
          GPG_KEY_ID=$(gpg --list-keys --with-colons | awk -F: '/^pub/{found=1} found && /^fpr/{print $10; exit}')
          echo "GPG_KEY_ID=$GPG_KEY_ID" >> "$GITHUB_ENV"

      - name: Download all release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          rm -rf repo/pool/
          mkdir -p repo/pool/main/c/ccs
          REPO="${{ github.repository }}"
          for tag in $(gh release list --repo "$REPO" --limit 100 --json tagName -q '.[].tagName'); do
            gh release download "$tag" --repo "$REPO" --pattern '*.deb' --dir repo/pool/main/c/ccs/ || true
          done
          ls -la repo/pool/main/c/ccs/

      - name: Generate APT repo metadata
        run: |
          cd repo
          mkdir -p dists/stable/main/binary-all

          dpkg-scanpackages --arch all pool/ > dists/stable/main/binary-all/Packages
          gzip -k -f dists/stable/main/binary-all/Packages

          cd dists/stable
          apt-ftparchive release \
            -o APT::FTPArchive::Release::Origin="ccs" \
            -o APT::FTPArchive::Release::Label="ccs" \
            -o APT::FTPArchive::Release::Suite="stable" \
            -o APT::FTPArchive::Release::Codename="stable" \
            -o APT::FTPArchive::Release::Architectures="all" \
            -o APT::FTPArchive::Release::Components="main" \
            . > Release

      - name: Sign release
        run: |
          cd repo/dists/stable
          gpg --batch --yes --armor --detach-sign -o Release.gpg Release
          gpg --batch --yes --armor --clearsign -o InRelease Release

      - name: Export public key
        run: gpg --armor --export "$GPG_KEY_ID" > repo/pubkey.gpg

      - name: Create install page
        run: |
          REPO_URL="https://${{ github.repository_owner }}.github.io/ccs"
          cat > repo/index.html <<EOF
          <!DOCTYPE html>
          <html><head><title>ccs - Claude Code Session manager</title>
          <style>body{font-family:system-ui;max-width:700px;margin:4em auto;padding:0 1em;color:#e0e0e0;background:#1a1a2e}
          pre{background:#16213e;padding:1em;border-radius:6px;overflow-x:auto}code{color:#00d4aa}
          a{color:#4ea8de}h1{color:#fff}</style></head>
          <body><h1>ccs</h1><p>Claude Code Session manager.</p>
          <h2>Install</h2><pre><code>curl -fsSL ${REPO_URL}/pubkey.gpg \\
            | sudo gpg --dearmor -o /etc/apt/keyrings/ccs.gpg

          echo "deb [arch=all signed-by=/etc/apt/keyrings/ccs.gpg] ${REPO_URL} stable main" \\
            | sudo tee /etc/apt/sources.list.d/ccs.list

          sudo apt update && sudo apt install ccs</code></pre>
          <h2>Usage</h2><pre><code>ccs new [name]        Start a Claude Code session
          ccs ls                List sessions
          ccs a [name]          Attach (fuzzy match)
          ccs kill [name]       Kill a session
          ccs schedule add      Schedule a recurring job
          ccs log               View cron execution log</code></pre>
          <p><a href="https://github.com/${{ github.repository }}">GitHub</a></p></body></html>
          EOF

      - name: Push gh-pages
        run: |
          cd repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "Update APT repo — v${{ needs.release.outputs.version }}"
          git push --force "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" gh-pages
